services:
  gramello:
    build: .
    image: gramello:local
    env_file:
      - .env.local
    environment:
      APP_BASE_URL: ${APP_BASE_URL:-http://localhost:3000}
    ports:
      - "3000:3000"
    volumes:
      - nourish-data:/data
    restart: unless-stopped

volumes:
  nourish-data:
