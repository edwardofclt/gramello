# Public values only. Native builds do not use the hosted API.
# Browser only: leave blank for same-origin /api requests.
EXPO_PUBLIC_API_URL=
# Optional public Segment source write key. Unset/blank disables native analytics.
EXPO_PUBLIC_SEGMENT_WRITE_KEY=
